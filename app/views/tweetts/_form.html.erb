<%= form_with(model: tweett) do |form| %>
  <% if tweett.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tweett.errors.count, "error") %> prohibited this tweett from being saved:</h2>

      <ul>
        <% tweett.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
